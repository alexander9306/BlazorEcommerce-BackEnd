@page "/logout"

@inject IClienteDataService LoginDataService
@inject NavigationManager NavigationManager


@code {
    protected override async Task OnInitializedAsync()
    {
        await LoginDataService.Logout();
        NavigationManager.NavigateTo("/", true);
    }
}
